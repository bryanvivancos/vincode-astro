---
import { getCollection, render } from 'astro:content';
import MainLayout from '../../layouts/MainLayout.astro';
import { ServicesPageTemplate } from '../../components/Services/ServicesPageTemplate';
import { BRAND_NAME } from '../../CONSTANTS';

export async function getStaticPaths() {
  const services = await getCollection('services');

  return services.map((service) => ({
    params: { id: service.slug },
    props: { service },
  }));
}

const { service } = Astro.props;
const { data } = service;
const { title, subtitle, description, demosTitle, benefits, projects, faqData } = data;

export const prerender = true
---

<MainLayout 
    title={`${title} | ${BRAND_NAME}`} 
    description={description} 
    url={`www.vincode.dev/services/${service.slug}`}>

    <ServicesPageTemplate
      title= {title}
      subtitles={subtitle}
      description={description}
      profits={benefits}
      demos={demosTitle}
      projects={projects} 
      faqsItems={faqData}
    />
</MainLayout>
